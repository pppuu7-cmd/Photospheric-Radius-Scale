name: RTK Stage 4D3 current correlated acceptance

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/rtk-stage4d3-correlated-acceptance-current.yml'

jobs:
  multiray:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '120'
      RTK_CACHE_KEY_VERSION: 'exact-float-v1'
      RTK_CORRELATED_IMPROVEMENT_TOL: '0.005'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rtk-class-build
      - name: Prepare cache-safe exact likelihood tree
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 --branch nonlocal https://github.com/dirian/class_public.git class_public
          git clone --depth 1 https://github.com/dscolnic/Pantheon.git class_public/pantheon
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          cp rtk/joint_profile_runner.py class_public/joint_profile_runner.py
          cp rtk/prepare_inference_core.py class_public/prepare_inference_core.py
          cp rtk/stage4d3_correlated_acceptance.py class_public/stage4d3_correlated_acceptance.py
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core.py
          ! grep -q 'round(float(p\[k\]),12)' inference_core.py
          ! grep -q 'A_s_ad\|n_s_ad' inference_core.py
      - name: Build CLASS
        working-directory: class_public
        run: make -j2 class
      - name: Install exact likelihood
        run: python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' numpy scipy
      - name: Download official Planck baseline
        working-directory: class_public
        run: |
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          mkdir planck_data
          tar -xzf planck.tar.gz -C planck_data
      - name: Run independent correlated multiray acceptance
        working-directory: class_public
        shell: bash
        run: |
          set -o pipefail
          python3 stage4d3_correlated_acceptance.py planck_data \
            284357.3770256277 0.6904692350128682 0.04683552900298674 0.25301548428566323 \
            2.083631213860309e-9 0.9644009156458047 7.218996521149225 \
            1050.045724929783 1050.0601104098123 | tee ../correlated_acceptance.log
          grep -q 'STAGE4D3_CORRELATED_ACCEPTANCE_' ../correlated_acceptance.log
          cat output/stage4d3_correlated_acceptance/correlated_acceptance_summary.json
      - name: Upload correlated acceptance artifact
        uses: actions/upload-artifact@v4
        with:
          name: rtk-stage4d3-current-correlated-acceptance
          path: |
            correlated_acceptance.log
            class_public/output/stage4d3_correlated_acceptance/correlated_acceptance_summary.json
            class_public/output/stage4d3_correlated_acceptance/correlated_acceptance_points.csv
          if-no-files-found: error
