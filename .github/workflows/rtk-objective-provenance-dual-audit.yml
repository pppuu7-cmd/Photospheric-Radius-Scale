name: RTK dual-objective provenance audit

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/rtk-objective-provenance-dual-audit.yml'

jobs:
  audit:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '300'
      RTK_CACHE_KEY_VERSION: 'exact-float-v1'
    steps:
      - uses: actions/checkout@v4
        with: {ref: rtk-class-build}
      - name: Prepare both objective cores
        run: |
          set -euo pipefail
          git clone --depth 1 --branch nonlocal https://github.com/dirian/class_public.git class_public
          git clone --depth 1 https://github.com/dscolnic/Pantheon.git class_public/pantheon
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          cp rtk/joint_profile_runner.py class_public/joint_profile_runner.py
          cp rtk/prepare_inference_core.py class_public/prepare_inference_core.py
          cp rtk/prepare_final_inference_core.py class_public/prepare_final_inference_core.py
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core_canonical.py
          python3 prepare_final_inference_core.py joint_profile_runner.py inference_core_final.py
      - name: Build CLASS
        working-directory: class_public
        run: make -j2 class
      - name: Install exact likelihood dependencies
        run: python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' numpy scipy
      - name: Download official Planck baseline
        working-directory: class_public
        run: |
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          mkdir planck_data && tar -xzf planck.tar.gz -C planck_data
      - name: Evaluate identical point under both objective definitions
        working-directory: class_public
        run: |
          python3 - <<'PY'
          import json,sys,importlib
          sys.argv=['audit','planck_data']
          C=importlib.import_module('inference_core_canonical')
          F=importlib.import_module('inference_core_final')
          p={'lam':284357.3770256277,'h':0.6904692350128682,'Ob':0.04683552900298674,'Om':0.25301548428566323,'As':2.083631213860309e-9,'ns':0.9644009156458047,'zre':7.218996521149225}
          c=C.evaluate('RTK',p); f=F.evaluate('RTK',p)
          if not c.get('ok') or not f.get('ok'): raise SystemExit({'canonical':c,'final':f})
          out={'point':p,'canonical':c,'final':f,'delta':{
            'score':f['score']-c['score'],
            'score_k01':f['score_k01']-c['score_k01'],
            'minus2logL_planck':-2*f['logL_planck']+2*c['logL_planck'],
            'chi2_BOSS_eff':f['chi2_BOSS_eff']-c['chi2_BOSS_eff'],
            'chi2_BOSS_k01':f['chi2_BOSS_k01']-c['chi2_BOSS_k01'],
            'chi2_SN':f['chi2_SN']-c['chi2_SN']}}
          open('../objective_provenance_audit.json','w').write(json.dumps(out,indent=2,sort_keys=True)+'\n')
          print(json.dumps(out['delta'],sort_keys=True))
          print('RTK_OBJECTIVE_PROVENANCE_AUDIT_PASS')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: rtk-objective-provenance-dual-audit
          path: objective_provenance_audit.json
          if-no-files-found: error
