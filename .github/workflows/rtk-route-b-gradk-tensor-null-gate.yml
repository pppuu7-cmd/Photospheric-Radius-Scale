name: RTK Route-B gradK tensor-null gate

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/rtk-route-b-gradk-tensor-null-trigger.txt'

permissions:
  contents: read

jobs:
  theorem:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rtk-class-build
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install exact symbolic runtime
        run: |
          python -m pip install --disable-pip-version-check 'sympy==1.14.0'

      - name: Run exact gradK tensor-null theorem
        run: |
          set -euo pipefail
          echo "RTK_SOURCE_SHA=$(git rev-parse HEAD)" | tee rtk_gradk_tensor_null_provenance.txt
          python -m py_compile rtk/route_b_gradK_tensor_null_gate.py
          python rtk/route_b_gradK_tensor_null_gate.py | tee rtk_gradk_tensor_null_gate.log
          grep -q '^RTK_ROUTE_B_GRADK_TENSOR_NULL_GATE_PASS ' rtk_gradk_tensor_null_gate.log

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rtk-route-b-gradk-tensor-null-gate
          path: |
            rtk_gradk_tensor_null_gate.log
            rtk_gradk_tensor_null_provenance.txt
          if-no-files-found: error
