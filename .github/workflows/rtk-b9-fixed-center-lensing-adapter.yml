name: RTK B9 fixed-center Planck lensing adapter

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/rtk-b9-fixed-center-lensing-adapter-trigger.txt'

permissions:
  contents: read

concurrency:
  group: rtk-b9-fixed-center-lensing-adapter
  cancel-in-progress: false

jobs:
  fixed-center-adapter:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '300'
      RTK_PLANCK_DATA: 'planck_data'
      RTK_CLASS_UPSTREAM_SHA: '36cf283628c4a3330ec9fd3d84239bf775f77317'
      RTK_PANTHEON_SHA: '7eb29dc87ba223b4ec8457cd3cccba1216c36fb7'
      RTK_PLANCK_SHA256: '0b73171e3acc671c28184466a45485a2d1c1d93676b832abdfe688c7b04024e6'
      RTK_NUMPY_VERSION: '2.5.2'
      RTK_SCIPY_VERSION: '1.18.0'
      RTK_PYTHON_VERSION: '3.12.3'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rtk-class-build
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.3'

      - name: Require frozen B9 protocol and adapter
        run: |
          set -euo pipefail
          grep -q 'FROZEN BEFORE THE FIRST RTK/LCDM COSMOLOGICAL STANDALONE-LENSING SCORE' research/robustness/B9_PLANCK_LENSING_ROBUSTNESS_PROTOCOL_v1.md
          python3 -m py_compile rtk/b9_fixed_center_lensing_adapter.py rtk/prepare_inference_core.py
          echo "research_source_commit=$(git rev-parse HEAD)" | tee b9_fixed_center_provenance.txt

      - name: Prepare pinned hardened CLASS tree
        run: |
          set -euo pipefail
          git clone --branch nonlocal https://github.com/dirian/class_public.git class_public
          git -C class_public reset --hard "$RTK_CLASS_UPSTREAM_SHA"
          git clone https://github.com/dscolnic/Pantheon.git class_public/pantheon
          git -C class_public/pantheon reset --hard "$RTK_PANTHEON_SHA"
          test "$(git -C class_public rev-parse HEAD)" = "$RTK_CLASS_UPSTREAM_SHA"
          test "$(git -C class_public/pantheon rev-parse HEAD)" = "$RTK_PANTHEON_SHA"
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_nonlocal_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          grep -q 'pba->V_prime_ini_nlde = 0.;' class_public/source/input.c
          cp rtk/joint_profile_runner.py rtk/prepare_inference_core.py rtk/b9_fixed_center_lensing_adapter.py class_public/
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core.py
          ! grep -q 'A_s_ad\|n_s_ad' inference_core.py
          ! grep -q 'round(float(p\[k\]),12)' inference_core.py
          grep -q 'RTK_PLANCK_DATA' inference_core.py

      - name: Build CLASS and install locked likelihood stack
        run: |
          set -euo pipefail
          make -C class_public -j2 class
          test "$(python3 -c 'import platform;print(platform.python_version())')" = "$RTK_PYTHON_VERSION"
          python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' "numpy==$RTK_NUMPY_VERSION" "scipy==$RTK_SCIPY_VERSION"
          python3 -m pip freeze | sort | tee b9_fixed_center_pip_freeze.txt

      - name: Download and verify Planck R3 baseline
        working-directory: class_public
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          echo "$RTK_PLANCK_SHA256  planck.tar.gz" | sha256sum -c -
          mkdir planck_data && tar -xzf planck.tar.gz -C planck_data
          test -d planck_data/baseline/plc_3.0/lensing/smicadx12_Dec5_ftl_mv2_ndclpp_p_teb_consext8.clik_lensing

      - name: Evaluate frozen LCDM and RTK lensing adapters
        working-directory: class_public
        run: |
          set -euo pipefail
          python3 b9_fixed_center_lensing_adapter.py | tee ../b9_fixed_center_lensing_adapter.log
          grep -q B9_FIXED_CENTER_LENSING_ADAPTER_CONTRACT_PASS ../b9_fixed_center_lensing_adapter.log
          python3 - <<'PY'
          import json
          s=json.load(open('../output/b9_fixed_center_lensing/summary.json'))
          assert s['classification']=='B9_FIXED_CENTER_LENSING_ADAPTER_CONTRACT_PASS'
          assert s['lmax']==[2500,2500,2500,-1,2500,-1,-1]
          assert s['default_par_len']==10005 and s['cl_payload_len']==10004
          for m in ('LCDM','RTK'):
              assert s['models'][m]['baseline_replay_abs_error']<=2e-6
          print('B9_FIXED_CENTER_ARTIFACT_PASS',s['models']['LCDM']['lensing_minus2loglike'],s['models']['RTK']['lensing_minus2loglike'])
          PY

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rtk-b9-fixed-center-lensing-adapter
          path: |
            b9_fixed_center_provenance.txt
            b9_fixed_center_pip_freeze.txt
            b9_fixed_center_lensing_adapter.log
            output/b9_fixed_center_lensing/summary.json
          if-no-files-found: warn
