name: RTK B4 neutrino LCDM recenter half stationarity

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/rtk-b4-neutrino-lcdm-recenter-half-trigger.txt'

permissions:
  contents: read

concurrency:
  group: rtk-b4-neutrino-lcdm-recenter-half
  cancel-in-progress: false

jobs:
  recentered-half-hessian:
    runs-on: ubuntu-24.04
    timeout-minutes: 300
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '300'
      RTK_PLANCK_DATA: 'planck_data'
      RTK_B4_STENCIL_SCALE: '0.5'
      RTK_CLASS_UPSTREAM_SHA: '36cf283628c4a3330ec9fd3d84239bf775f77317'
      RTK_PANTHEON_SHA: '7eb29dc87ba223b4ec8457cd3cccba1216c36fb7'
      RTK_PLANCK_SHA256: '0b73171e3acc671c28184466a45485a2d1c1d93676b832abdfe688c7b04024e6'
      RTK_NUMPY_VERSION: '2.5.2'
      RTK_SCIPY_VERSION: '1.18.0'
      RTK_PYTHON_VERSION: '3.12.3'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rtk-class-build
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.3'

      - name: Require passed recentered base gate
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          t=json.load(open('research/robustness/b4_neutrino_lcdm_recenter_target_v1.json'))
          b=json.load(open('research/robustness/b4_neutrino_lcdm_recenter_base_result_v1.json'))
          assert b['classification']=='B4_NEUTRINO_LCDM_RECENTER_BASE_PASS'
          assert b['run_id']==32251647845 and b['artifact_id']==9368193099
          assert b['best_improvement_eff']<=b['recenter_tolerance_S']==0.005
          assert b['positive_definite_eff'] is True and b['min_eigenvalue_eff']>1e-8
          assert b['center']==t['models']['LCDM']['center']
          assert t['proof_logic']['validation_scale']==0.5
          print('B4_LCDM_HALF_PREREQUISITES_PASS')
          PY
          cp research/robustness/b4_neutrino_lcdm_recenter_target_v1.json research/robustness/b4_neutrino_stationarity_targets_v1.json
          python3 -m py_compile rtk/neutrino_stationarity_hessian.py rtk/prepare_inference_core.py

      - name: Prepare pinned hardened CLASS tree
        run: |
          set -euo pipefail
          echo "research_source_commit=$(git rev-parse HEAD)" | tee b4_lcdm_half_provenance.txt
          git clone --branch nonlocal https://github.com/dirian/class_public.git class_public
          git -C class_public reset --hard "$RTK_CLASS_UPSTREAM_SHA"
          git clone https://github.com/dscolnic/Pantheon.git class_public/pantheon
          git -C class_public/pantheon reset --hard "$RTK_PANTHEON_SHA"
          test "$(git -C class_public rev-parse HEAD)" = "$RTK_CLASS_UPSTREAM_SHA"
          test "$(git -C class_public/pantheon rev-parse HEAD)" = "$RTK_PANTHEON_SHA"
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_nonlocal_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          cp rtk/joint_profile_runner.py rtk/prepare_inference_core.py rtk/neutrino_stationarity_hessian.py class_public/
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core.py
          ! grep -q 'A_s_ad\|n_s_ad' inference_core.py
          ! grep -q 'round(float(p\[k\]),12)' inference_core.py
          grep -q 'RTK_PLANCK_DATA' inference_core.py

      - name: Build CLASS and locked runtime
        run: |
          set -euo pipefail
          make -C class_public -j2 class
          test "$(python3 -c 'import platform;print(platform.python_version())')" = "$RTK_PYTHON_VERSION"
          python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' "numpy==$RTK_NUMPY_VERSION" "scipy==$RTK_SCIPY_VERSION"
          python3 -m pip freeze | sort | tee b4_lcdm_half_pip_freeze.txt

      - name: Download and verify Planck baseline
        working-directory: class_public
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          echo "$RTK_PLANCK_SHA256  planck.tar.gz" | sha256sum -c -
          mkdir planck_data && tar -xzf planck.tar.gz -C planck_data

      - name: Run exact LCDM half-scale Hessian
        working-directory: class_public
        run: |
          set -euo pipefail
          python3 neutrino_stationarity_hessian.py LCDM | tee ../b4_lcdm_recenter_half.log
          grep -q 'B4_NEUTRINO_STATIONARITY_HESSIAN_COMPLETE LCDM 0.5' ../b4_lcdm_recenter_half.log
          python3 - <<'PY'
          import json
          s=json.load(open('../output/b4_neutrino_stationarity/lcdm/scale_0.5/summary.json'))
          t=json.load(open('../research/robustness/b4_neutrino_lcdm_recenter_target_v1.json'))
          assert s['center']==t['models']['LCDM']['center']
          assert s['stencil_scale']==0.5
          assert s['eff']['best_improvement']<=0.005
          assert s['eff']['positive_definite'] is True
          print('B4_LCDM_RECENTER_HALF_PASS',s['eff']['best_exact_S'],s['eff']['best_improvement'],min(s['eff']['eigenvalues_y']))
          PY

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rtk-b4-neutrino-lcdm-recenter-half
          path: |
            b4_lcdm_recenter_half.log
            b4_lcdm_half_provenance.txt
            b4_lcdm_half_pip_freeze.txt
            output/b4_neutrino_stationarity/lcdm/scale_0.5/
          if-no-files-found: warn
