name: RTK Stage 4D3 Round5 ultrafine 7D stationarity

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/rtk-stage4d3-round5-ultrafine-stationarity.yml'

jobs:
  stationarity:
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - {mapping: eff, scale: '0.0625', expected: '1050.0338294787382'}
          - {mapping: k01, scale: '0.0625', expected: '1050.0482111660676'}
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '240'
      RTK_CACHE_KEY_VERSION: 'exact-float-v1'
      RTK_STATIONARITY_GRAD_TOL: '0.03'
      RTK_STATIONARITY_IMPROVEMENT_TOL: '0.005'
    steps:
      - uses: actions/checkout@v4
        with: {ref: rtk-class-build}
      - name: Prepare exact cache-safe tree
        run: |
          set -euo pipefail
          git clone --depth 1 --branch nonlocal https://github.com/dirian/class_public.git class_public
          git clone --depth 1 https://github.com/dscolnic/Pantheon.git class_public/pantheon
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          cp rtk/joint_profile_runner.py class_public/joint_profile_runner.py
          cp rtk/prepare_inference_core.py class_public/prepare_inference_core.py
          cp rtk/stage4d3_joint_stationarity.py class_public/stage4d3_joint_stationarity.py
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core.py
          ! grep -q 'round(float(p\[k\]),12)' inference_core.py
      - name: Build CLASS
        working-directory: class_public
        run: make -j2 class
      - name: Install exact likelihood dependencies
        run: python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' numpy scipy
      - name: Download official Planck baseline
        working-directory: class_public
        run: |
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          mkdir planck_data && tar -xzf planck.tar.gz -C planck_data
      - name: Run ultrafine exact 7D stationarity stencil
        working-directory: class_public
        run: |
          set -o pipefail
          python3 stage4d3_joint_stationarity.py planck_data '${{ matrix.mapping }}' \
            '217225.01601516694' \
            '0.6904831253428524' '0.046836300417955265' '0.25300743080221694' \
            '2.0837288833768707e-9' '0.9643603115669437' '7.21843542110055' \
            '${{ matrix.expected }}' '${{ matrix.scale }}' | tee ../stationarity_${{ matrix.mapping }}_${{ matrix.scale }}.log
          grep -q 'STAGE4D3_JOINT_STATIONARITY_' ../stationarity_${{ matrix.mapping }}_${{ matrix.scale }}.log
      - uses: actions/upload-artifact@v4
        with:
          name: rtk-stage4d3-round5-ultrafine-${{ matrix.mapping }}
          path: |
            stationarity_${{ matrix.mapping }}_${{ matrix.scale }}.log
            class_public/output/stage4d3_joint_stationarity/${{ matrix.mapping }}/**/joint_stationarity_summary.json
            class_public/output/stage4d3_joint_stationarity/${{ matrix.mapping }}/**/joint_stationarity_points.csv
          if-no-files-found: error
