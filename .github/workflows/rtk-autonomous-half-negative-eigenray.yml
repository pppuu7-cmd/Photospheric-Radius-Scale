name: RTK autonomous exact half-stencil negative-eigenray gate

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rtk-autonomous-half-negative-eigenray
  cancel-in-progress: false

jobs:
  half-negative-eigenray:
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    env:
      CLIPY_NOJAX: '1'
      OMP_NUM_THREADS: '2'
      RTK_CLASS_TIMEOUT: '300'
      RTK_EIGENRAY_SOURCE: 'half'
      RTK_CLASS_UPSTREAM_SHA: '36cf283628c4a3330ec9fd3d84239bf775f77317'
      RTK_PANTHEON_SHA: '7eb29dc87ba223b4ec8457cd3cccba1216c36fb7'
      RTK_PLANCK_SHA256: '0b73171e3acc671c28184466a45485a2d1c1d93676b832abdfe688c7b04024e6'
      RTK_NUMPY_VERSION: '2.5.2'
      RTK_SCIPY_VERSION: '1.18.0'
      RTK_PYTHON_VERSION: '3.12.3'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rtk-class-build
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.3'
      - name: Prepare pinned hardened exact-likelihood tree
        run: |
          set -euo pipefail
          test "$(python3 -c 'import platform;print(platform.python_version())')" = "$RTK_PYTHON_VERSION"
          git clone --branch nonlocal https://github.com/dirian/class_public.git class_public
          git -C class_public reset --hard "$RTK_CLASS_UPSTREAM_SHA"
          git clone https://github.com/dscolnic/Pantheon.git class_public/pantheon
          git -C class_public/pantheon reset --hard "$RTK_PANTHEON_SHA"
          test "$(git -C class_public rev-parse HEAD)" = "$RTK_CLASS_UPSTREAM_SHA"
          test "$(git -C class_public/pantheon rev-parse HEAD)" = "$RTK_PANTHEON_SHA"
          cp rtk/khronon_background.c rtk/khronon_perturbations.c class_public/source/
          cp rtk/khronon_background.h rtk/khronon_perturbations.h class_public/include/
          python3 rtk/apply_rtk_class_patch.py class_public
          python3 rtk/upgrade_rtk_inputs.py class_public
          python3 rtk/upgrade_rtk_nonlocal_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_initial_conditions.py class_public
          python3 rtk/upgrade_rtk_matter_sources.py class_public
          python3 rtk/upgrade_rtk_gamma_root.py class_public
          python3 rtk/upgrade_rtk_spectra_endpoint.py class_public
          grep -q 'pba->V_prime_ini_nlde = 0.;' class_public/source/input.c
          cp rtk/joint_profile_runner.py rtk/prepare_inference_core.py rtk/autonomous_negative_eigenray_gate.py class_public/
          cp rtk/data/boss_DR12Consensus_final.dat rtk/data/final_consensus_covtot_dM_Hz_fsig.txt class_public/
          cd class_public
          python3 prepare_inference_core.py joint_profile_runner.py inference_core.py
          ! grep -q 'round(float(p\[k\]),12)' inference_core.py
          grep -q "if r.get('ok')" inference_core.py
      - name: Build CLASS and install pinned likelihood dependencies
        run: |
          set -euo pipefail
          make -C class_public -j2 class
          python3 -m pip install --disable-pip-version-check 'clipy-like==0.15' "numpy==$RTK_NUMPY_VERSION" "scipy==$RTK_SCIPY_VERSION"
          python3 - <<'PY'
          import numpy,scipy,os,platform
          assert platform.python_version()==os.environ['RTK_PYTHON_VERSION']
          assert numpy.__version__==os.environ['RTK_NUMPY_VERSION']
          assert scipy.__version__==os.environ['RTK_SCIPY_VERSION']
          PY
          python3 -m pip freeze | sort | tee half_negative_eigenray_pip_freeze.txt
          python3 --version | tee half_negative_eigenray_python_version.txt
      - name: Download and verify official Planck baseline
        working-directory: class_public
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 5 -o planck.tar.gz 'https://pla.esac.esa.int/pla/aio/product-action?COSMOLOGY.FILE_ID=COM_Likelihood_Data-baseline_R3.00.tar.gz'
          sha256sum planck.tar.gz | tee ../half_negative_eigenray_planck_sha256.txt
          echo "$RTK_PLANCK_SHA256  planck.tar.gz" | sha256sum -c -
          mkdir planck_data && tar -xzf planck.tar.gz -C planck_data
      - name: Run exact half-stencil negative-eigenray gate
        working-directory: class_public
        run: |
          set -euo pipefail
          python3 autonomous_negative_eigenray_gate.py | tee ../half_negative_eigenray.log
          grep -q RTK_AUTONOMOUS_NEGATIVE_EIGENRAY_GATE_COMPLETE ../half_negative_eigenray.log
          python3 - <<'PY'
          import json
          s=json.load(open('output/autonomous_negative_eigenray_gate/summary.json'))
          assert s['eigenray_source']=='half'
          assert abs(float(s['source_stencil_scale'])-0.5)<1e-15
          print('RTK_HALF_NEGATIVE_EIGENRAY_SOURCE_CONFIRMED')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rtk-autonomous-half-negative-eigenray
          path: |
            half_negative_eigenray.log
            half_negative_eigenray_pip_freeze.txt
            half_negative_eigenray_python_version.txt
            half_negative_eigenray_planck_sha256.txt
            class_public/output/autonomous_negative_eigenray_gate/summary.json
            class_public/output/autonomous_negative_eigenray_gate/provenance.json
            class_public/output/autonomous_negative_eigenray_gate/points.jsonl
            class_public/output/autonomous_negative_eigenray_gate/failures.jsonl
          if-no-files-found: warn
