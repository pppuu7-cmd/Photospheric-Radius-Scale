# RTK continuation checkpoint — 2026-08-24 14:08 UTC

## Scope

This checkpoint is the recovery anchor for the current A5/B4/B5 frontier. It records scientific state, frozen decisions, and pending result paths; workflow absence is never interpreted as scientific FAIL.

## A5 LCDM cross-basin frontier

The exact old→new line audit showed that the historical accepted-score LCDM point was not Hessian-certified and that the original B9-derived `t=1` seed was itself superseded. The completed frozen forward continuation from `t=1.1` now gives:

- result: `research/robustness/A5_LCDM_T1P1_FORWARD_LINE_CONTINUATION_RESULT_v1.json`;
- classification: `A5_LCDM_T1P1_FORWARD_LINE_CONTINUATION_COMPLETE`;
- decision: `T1P1_FORWARD_CLEAR_STATIONARITY_REQUIRED`;
- exact best sampled position: `t=1.1`;
- exact score: `1049.3633203461366`;
- replay error at `t=1.1`: `2.2737367544323206e-13`;
- forward improvement beyond `t=1.1`: `0.0` on the frozen grid through `t=1.9`.

Therefore this one-dimensional discovered direction is recenter-clear. The conditional `t=1.9` extension target is not eligible and must not execute.

The exact `t=1.1` center is

- `As=2.0983938219102527e-09`
- `Ob=0.04838888999978602`
- `Om=0.2584441959848294`
- `h=0.6805163510946404`
- `lambda_D=0`
- `ns=0.96638264917319`
- `zre=7.674455444668947`.

It improves the historical LCDM exact score by `0.6027980016244774`. With the still-certified historical RTK exact point, the navigation-only mixed delta is `+0.8865920836503844`. This is not a frozen paired result.

### New mandatory stationarity chain

Frozen before new Hessian scores:

`research/robustness/A5_LCDM_T1P1_STATIONARITY_TARGET_v1.json`

Base steps are unchanged from the previous A5 LCDM local protocol: `h=0.00035`, `Ob=0.00007`, `Om=0.0007`, `As=4e-12`, `ns=0.00035`, `zre=0.07`.

Control workflow:

`main:.github/workflows/rtk-a5-lcdm-t1p1-stationarity-base.yml`

Trigger commit:

`6826301eed67b1185cc0b78a5f81c032f3fb3317`

Expected persisted result:

`research/robustness/A5_LCDM_T1P1_BASE_RESULT_v1.json`

Frozen base decision tree:

1. best exact improvement `>0.005` => mandatory recenter at the exact best point before any other scale;
2. recenter-clear + PD => independent half-scale Hessian at the identical center;
3. recenter-clear + non-PD => freeze exact negative/soft eigenmode rays before any smaller-scale fallback.

At the checkpoint time the base result file was not yet present. This is PENDING, not FAIL.

## Symmetric RTK A5 audit

The LCDM historical semantics issue does not occur on the final RTK certificate. RTK accepted-score params coincide exactly with the final base and half Hessian centers; both final stencils have best improvement `0.0` and positive-definite curvature. A3 therefore remains locally closed.

## B4 minimal-neutrino branch

Objective remains separate: `matched-ultra-linstep2+dense-BOSS+nu0p06-additive-v1`.

Current recovered v4 center:

- `S_center=1050.5511025943172`;
- half best improvement approximately `4.9814518433777266e-05`;
- half Hessian non-PD with minimum eigenvalue approximately `-4.7214683209037513e-05`.

The mandatory gate is the already-frozen full-eigenvector half-scale ray audit. Expected persisted result:

`research/robustness/B4_NEUTRINO_RTK_RECENTER_V4_HALF_EIGENMODE_RAYS_RESULT_v1.json`

At checkpoint time that file was absent. No scientific classification is inferred. If the exact ray improvement exceeds `0.005`, freeze a new recenter target. If no descent exceeds `0.005`, only then may the preregistered v4 quarter-scale Hessian execute.

## B5 survey/nonlinear RSD branch

The first B5 subgate is already frozen and intended to compare the production growth definition `fσ8_eff=dσ8/dln(a)` with `f(k,z)σ8` over the actual BOSS Fourier range `k=0.02…0.24 h/Mpc` at `z=0.38,0.51,0.61`.

Expected result:

`research/robustness/B5_BOSS_LINEAR_SCALE_DEPENDENCE_RESULT_v1.json`

At checkpoint time it was absent. Even a PASS can close only the linear scale-dependence subgate. Survey-window convolution, Alcock–Paczynski remapping, nonlinear RSD modeling and nuisance/bias marginalization remain separately scoped.

## Locked interpretation

- no global-minimum claim;
- no score comparison across different objectives;
- no sigma/Wilks/AIC/BIC/Bayes reinterpretation from raw score differences;
- workflow success/failure is not a scientific classification;
- Hessian certificates apply only to their exact center;
- B9 and B10 protocol-v1 remain closed and are not to be repeated without a new frozen question.
