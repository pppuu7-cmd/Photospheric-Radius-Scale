# RTK continuation checkpoint — 2026-08-24 23:05 UTC

Status: current-lineage recovery checkpoint. Frozen theorem/result files override this summary if any discrepancy appears.

## C10 Newtonian DAE frontier

- `research/theory_results/RTK_C10_U1_NEWTONIAN_DAE_SOLVE_BASIS_RESULT_v1.json` is authoritative.
- Classification: `C10_U1_NEWTONIAN_3X3_ALGEBRAIC_SOLVE_BASIS_HAS_ONE_POSITIVE_CROSSING_SCOPED`.
- In the frozen flat-FLRW, k>0, lambda_HL>1, Eth_IR_full=2 IR solve basis,
  `det = 2 L Delta(x)`, `L=-x`, `x=k^2`, with `Delta=r x^2+b x+c`, `r=lambda_HL-1>0`, and `c<0` on the positive matter/source domain.
- Therefore the two real roots of Delta have opposite signs: exactly one positive solve-basis crossing exists.
- This is NOT physical rank loss: the independently certified preferred-foliation/all-q physical constraint-rank determinant is a different object.
- Software consequence: do not globally invert the Newtonian 3x3 algebraic matrix. Next admissible C10 question is DAE/null-space compatibility at the positive crossing or a crossing-regular preferred-coordinate solve with Newtonian potentials as outputs.

Historical action-fluid robustness remains green at the replay-certified RTK point:
- twin spectra max normalized difference ~`3.538390324840441e-11`;
- fixed-parameter action-shadow minus production raw objective `1.8189894035458565e-10`.
These do not constitute a completed-U1 metric likelihood fit.

## B4 minimal-neutrino frontier

- Rerun `32765132546` ended workflow-failure only in the persistence step. Exact ray science step succeeded.
- Artifact `9534706258`, digest `sha256:08aefaa5a42001c4daf799a11f3b16f39dcc029a466d901eba7682a2a2239da0`, was recovered.
- Durable result restored as `research/robustness/B4_NEUTRINO_RTK_RECENTER_V4_HALF_EIGENMODE_RAYS_RESULT_v1.json`.
- Classification: `B4_RECENTER_V4_HALF_EIGENMODE_RAYS_NO_DESCENT_GT_0P005`.
- Center replay error `0.0`; max exact improvement `-3.6337555684440304e-06`; every frozen ray is worse than center.
- `research/state/B4_current.json` advanced to quarter-scale required.
- The quarter target was preregistered before ray scores and uses the unchanged v4 center. Main dispatcher successfully launched run `32787061934`; run is in progress at this checkpoint.
- Quarter decision tree remains frozen: >0.005 improvement -> recenter; recenter-clear/non-PD -> exact eigenmode rays before smaller scale; recenter-clear/PD -> independent fresh-tree replay before local B4 closure.

## B5 linear BOSS-scale frontier

- Prior rerun `32765115975` failed inside RTK CLASS execution before any science result; this is not a scientific FAIL.
- Artifact contained no physical result. Worker audit found an execution-path defect: the worker created `../output/b5_boss_linear_scale_dependence` but CLASS was configured with root `output/b5_boss_linear_scale_dependence/...` inside the CLASS tree.
- Only this output-root mismatch was corrected in `rtk/b5_boss_linear_scale_dependence.py`; frozen k/z grid, RTK/LCDM points, objective context and acceptance thresholds are unchanged.
- Frozen target rerun launched as run `32787238790`; in progress at this checkpoint.

## A5

- No new `recenter1` scientific result was persisted during this continuation. Do not duplicate or infer PASS/FAIL from absence.

## Strict next order

1. Read B4 quarter result when persisted and follow its frozen branch.
2. Read B5 rerun result; if execution succeeds, classify only from its frozen thresholds and keep B5-SURVEY separate.
3. Freeze C10 positive-crossing DAE/null-space compatibility theorem before evaluating it. Do not invert the 3x3 Newtonian matrix through its positive root.
4. Continue A5 recenter1 chain only from persisted science artifacts.
5. Keep B9/B10 v1 closed.
