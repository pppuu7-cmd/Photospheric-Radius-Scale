# RTK Formula Bible — current pointer

Updated: 2026-08-23 22:05 UTC
Status: **CURRENT RECOVERY POINTER — read this file first**

This pointer is updated in place so a new chat or researcher has one stable file naming the freshest durable state.

## Canonical reading order

1. `research/methods/RTK_FORMULA_BIBLE.md` — base notation and production identities.
2. `research/methods/RTK_FORMULA_BIBLE_C8_U1_COMPLETION_ADDENDUM_2026-08-22T2220Z.md` — mandatory corrected physical Dirac order.
3. `research/methods/RTK_FORMULA_BIBLE_C8_U1_LOWK_RANK_ADDENDUM_2026-08-22T2245Z.md` — controlled punctured-low-k rank and finite-epsilon bridge.
4. `research/methods/RTK_FORMULA_BIBLE_C9_UV_O4_FRONTIER_2026-08-23T2155Z.md` — finite-Mc PPN/O4, P(X) partial waves/cutoff, B9 safety, quadratic curvature carrier and the earlier generic all-hard UV power count.
5. `research/methods/RTK_FORMULA_BIBLE_C9_N2_SOFT_S_O4_CORRECTION_2026-08-23T2205Z.md` — **MANDATORY LATEST CORRECTION**: exact n=2 Q3/Q4 kernels, soft-spatial s-channel warning, and generalized nonlocal O(4) potential `Psi_res`. This file supersedes any more optimistic n=2 interpretation in item 4.
6. `research/checkpoints/RTK_MODEL_DEVELOPMENT_ADDENDUM_2026-08-23T2205Z.md` — **current run/artifact/digest checkpoint and continuation queue**.

Older appendices remain useful for derivations and provenance, but items 2-6 control any conflicting interpretation.

## Non-negotiable supersession rules

- Eliminate the genuine auxiliary pair `(p_Q,C_Lambda)` before using the reduced physical U(1) chain. The old assumed 8x8 basis is not the coupled DOF proof.
- Exact flat-FLRW `k=0` is background/source inheritance, not a propagating-rank certificate. Use a punctured interval `0<|k|<epsilon`.
- Keep `M_c>0` symbolic until rank and PPN/scale-window gates are intersected. Do not fit it as a substitute for structural closure.
- The elliptic compensator filters ordinary universally coupled matter `H0`; the neutral RTK scalar is separate.
- Finite-Mc O(3) gives `alpha1=8 M_c^2/k^2`, but the O(4) sector is not obtained by the naive substitution `a1->f(k)` in the parent closed-form PPN formulas.
- The finite P(X)-only partial-wave cutoff is a single-channel tree boundary, not the all-sector EFT cutoff.
- The generic all-hard curvature theorem `g a_l~k^(1-n)` remains valid in its scope, but **does not control the elastic soft-spatial s-channel** found for n=2.
- `D_iR3 D^iR3` is therefore **YELLOW / conditional**, not a promoted or preferred final UV completion, until full lapse/shift/state-function reduction tests whether its nonzero bare `K3_s=-96k^6` cancels.
- C9 technical naturalness remains open; radiative stability of the exceptional U(1) surface is not assumed.

## Current leading formulas

### Matter compensator / rank

`f(k)=k^2/(M_c^2+k^2)`.

On the controlled flat branch,

`B_RTK/q=[[A,-b],[b,0]]`,

`Delta B_m/q=(1/M_c^2)[[0,-x],[x,0]]`,

`det L=(b+x/M_c^2)^2`.

### Projectable finite-Mc PPN

O(2): `gamma_PPN=1`, `G_N(k)=G f(k)`.

O(3):

`alpha1=8 M_c^2/k^2`,

`alpha2-zeta1+2xi = 2 M_c^2(2lambda_HL-1)/[k^2(lambda_HL-1)]`.

Symbolic alpha1 + 1% cosmology window:

`99 k_cos^2 <= M_c^2 <= eps1 k_local^2/8`.

### O(4) generalized potential

The new finite-Mc source is controlled by

`delta f=-(1/M_c^2)L^-1 delta(D^2)L^-1`.

If `S_res` is its additive source in the published projectable O(4) `A4` equation, then on `a1=1`

`-2 Delta delta A4=S_res`,

`delta h00=-Delta^-1 S_res`.

Define

`Psi_res=-Delta^-1 S_res`.

The exact resolvent kernel is non-separable in output/internal momenta, so for arbitrary extended sources `Psi_res` is generically a genuine mode-mixing potential rather than a constant `Delta beta`.

### P(X)-only partial-wave boundary

`g(k)=k^2/[2 omega(k)^2 v_g(k)]`, `tilde a_l=g a_l`, with tree bound `|Re tilde a_l|<=1/2`.

Production rational RTK:

`g=(1+k^2/M_K^2)^(5/2)/(2 c_a^3)`.

Certified early P(X)-only cutoff plateau:

`k_unit,early=1.9807199478328038e-4 eV`.

The conservative B9 envelope remains below this single-channel cutoff by at least `8.963423475287682e15` in momentum through `z=1e9`.

### n=2 curvature carrier — corrected status

Quadratic candidate:

`Delta S_UV = integral N sqrt(gamma) alpha6 D_iR3 D^iR3`,

`alpha6=-G/[32 H^2 M_U^4]`,

`omega^2=c_a^2 k^2(1+k^4/M_U^4)/(1+k^2/M_K^2)`.

Exact bare conformal elastic kernels include

`K3_s=-96 k^6`,

`K4=320 k^6(9-2 cos^2 theta)`.

The soft-s partial wave behaves as

`M_U<<k<<M_K:  g a0_s -> -3 H^2 M_U^2/[128 pi K c_a]`  (constant),

`k>>M_K,M_U:  g a0_s -> -9 H^2 M_U^2 k/[256 pi K c_a M_K]`  (linear growth).

This is a warning in the fixed-lapse conformal sector, not a final no-go. The decisive question is whether the full nonlinear constraints/state-function terms cancel the soft-s vertex.

## Immediate order

1. Derive the full cubic lapse/shift constraints with the n=2 carrier and a fully specified nonlinear `alpha6` completion; test directly whether the reduced `q_s=0` cubic vertex cancels.
2. If the soft-s vertex survives, quantify it over the frozen background and compare higher-order or symmetry-protected carriers rather than forcing n=2.
3. In parallel, evaluate `S_res`/`Psi_res` for the controlled uniform-sphere source and solve the modified O(4) `A4` equation to obtain a source-specific observable.
4. Intersect rank, finite-Mc PPN and any viable UV windows symbolically; do not choose `M_c` or `M_U` by fit before structural closure.
5. Continue technical-naturalness/RG and strong-field/compact-object gates before any claim of a complete theory.

For B9 statistical work, follow the separately frozen B9 decision tree; do not retune structural C8/C9 scales through the B9 fit.
