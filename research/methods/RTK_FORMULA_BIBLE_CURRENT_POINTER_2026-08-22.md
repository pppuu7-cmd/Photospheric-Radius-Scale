# RTK Formula Bible — current pointer

Updated: 2026-08-23 21:55 UTC
Status: **CURRENT RECOVERY POINTER — read this file first**

This pointer is intentionally updated in place so a new chat or researcher has one stable file that always names the freshest durable research state.

## Canonical reading order

1. `research/methods/RTK_FORMULA_BIBLE.md` — base notation and production identities.
2. `research/methods/RTK_FORMULA_BIBLE_C8_U1_COMPLETION_ADDENDUM_2026-08-22T2220Z.md` — mandatory correction to the physical Dirac order: genuine auxiliary pair first, exact projection, reduced chain, zero-mode caveat.
3. `research/methods/RTK_FORMULA_BIBLE_C8_U1_LOWK_RANK_ADDENDUM_2026-08-22T2245Z.md` — controlled flat-FLRW punctured-low-k rank, filtered-matter one-coefficient reduction, leading determinant and finite-epsilon bridge.
4. `research/methods/RTK_FORMULA_BIBLE_C9_UV_O4_FRONTIER_2026-08-23T2155Z.md` — **current scientific frontier**: finite-Mc O(2)/O(3), one-resolvent O(4) source structure, exact P(X) tree partial waves and cutoff, B9 k safety, higher-spatial UV window, intrinsic-curvature carrier and n=2 power counting.
5. `research/checkpoints/RTK_MODEL_DEVELOPMENT_ADDENDUM_2026-08-23T2155Z.md` — **current run/artifact/digest checkpoint and continuation queue**.

Older Formula Bible appendices remain useful for derivations and provenance, but items 2-5 above control any conflicting interpretation.

## Non-negotiable supersession rules

- Do **not** use the old assumed `old-four + auxiliary-four` 8x8 basis as the physical coupled DOF proof. Eliminate `(p_Q,C_Lambda)` first.
- Do **not** use exact flat-FLRW `k=0` as a local propagating-rank certificate. The physical local theorem is on a punctured interval `0<|k|<epsilon`.
- Keep `M_c>0` symbolic until rank and PPN/scale-window gates are intersected. A fit is not a structural proof.
- The elliptic compensator filters ordinary universally coupled matter `H0`; the neutral RTK scalar is separate and must not be inserted into that filtered source density.
- The finite-Mc O(3) projectable result gives `alpha1=8 M_c^2/k^2`; it does not by itself split the full O(4) PPN sector.
- The finite P(X)-only partial-wave cutoff is a **single-channel tree boundary**, not the final all-sector EFT cutoff.
- The current preferred UV candidate `D_i R3 D^i R3` is **not yet promoted**. It has a GREEN quadratic carrier theorem and favorable n=2 power counting, but its exact reduced cubic/quartic amplitude and full constraint algebra are still open.
- C9 technical naturalness remains open; the exceptional U(1) scalar-graviton-free surface is not assumed radiatively stable.

## Current leading formulas

### Matter compensator / rank

`f(k)=a_eff=k^2/(M_c^2+k^2)`.

On the controlled flat branch,

`B_RTK/q=[[A,-b],[b,0]]`,

`Delta B_m/q=(1/M_c^2)[[0,-x],[x,0]]`,

`det L=(b+x/M_c^2)^2`.

For isotropic ordinary matter,

`M_c^2 > 2|sum_s(rho_s+d p_s/2)|/[|eta0|(d-1)M_Pl^2]`

is a conservative leading no-cancellation condition.

### Projectable finite-Mc PPN

Static O(2):

`gamma_PPN=1`,

`G_N(k)=G f(k)`.

O(3):

`alpha1=8(1/f-1)=8 M_c^2/k^2`,

`alpha2-zeta1+2xi = 2 M_c^2(2lambda_HL-1)/[k^2(lambda_HL-1)]`.

The combined symbolic alpha1 + 1% cosmology window is

`99 k_cos^2 <= M_c^2 <= eps1 k_local^2/8`.

### O(4) nonlocal structure

The new finite-Mc O(4) metric/source nonlocality is controlled by one object:

`delta f = -(1/M_c^2)L^-1 delta(D^2)L^-1`.

The full PPN coefficient solve is pending.

### P(X)-only partial-wave boundary

For isotropic `omega(k)`,

`g(k)=k^2/[2 omega(k)^2 v_g(k)]`,

`tilde a_l=g a_l`,

`|Re tilde a_l|<=1/2` at tree level.

For the production rational RTK dispersion,

`g=(1+k^2/M_K^2)^(5/2)/(2 c_a^3)`.

Certified early plateau:

`k_unit,early=1.9807199478328038e-4 eV`.

The conservative B9 envelope remains below the single-channel cutoff by at least

`8.963423475287682e15` in momentum through `z=1e9`.

### Current preferred UV candidate for testing

`Delta S_UV = integral N sqrt(gamma) alpha6 D_i R3 D^i R3`,

`alpha6=-G/[32 H^2 M_U^4]`,

producing at quadratic scalar order

`omega^2=c_a^2 y(1+y^2/M_U^4)/(1+y/M_K^2)`.

For the curvature-carrier family `[D^(n-1)R3]^2`,

`g a_l ~ k^(1-n)`.

Thus n=2 is the current minimal asymptotically soft candidate (`~k^-1`) to test nonlinearly.

## Immediate order

1. Derive the exact cubic and quartic scalar expansion of `D_i R3 D^i R3` on the flat rolling patch.
2. Add lapse/shift perturbations and solve the nonlinear constraints for the n=2 carrier.
3. Compute exact P(X)+n=2 2->2 amplitudes/partial waves including interference.
4. In parallel, assemble and solve the complete finite-Mc O(4) projectable source/PPN coefficient system using the certified single resolvent derivative.
5. Intersect rank, PPN and UV scale windows symbolically; do not choose `M_c` or `M_U` by fit before structural closure.
6. Continue technical-naturalness/RG and strong-field/compact-object gates before any claim of a complete theory.

For B9 statistical work, continue to follow the separately frozen B9 decision tree. Do not retune the C8/C9 structural scales through the B9 fit.
