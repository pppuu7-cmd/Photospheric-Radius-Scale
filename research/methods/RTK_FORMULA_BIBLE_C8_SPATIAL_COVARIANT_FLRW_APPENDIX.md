# RTK Formula Bible — C8 Spatial-Covariant FLRW Exact-Match Appendix

Date: 2026-08-21
Status: **YELLOW pending CI artifact inspection; exact symbolic identities independently checked before commit**

## Purpose

This appendix derives the quadratic scalar FLRW kernel of the already existing local spatial-covariant metric+Khronon benchmark and connects it directly to the production DBI-Khronon background formulas.

The result is conceptually different from the BPS pole-only embedding. Instead of producing the RTK rational dispersion through a rational spatial term and then repairing a residue mismatch, the local acceleration operator produces the RTK momentum-dependent kinetic factor directly.

The candidate unitary-gauge action is

`S = ∫ dt d^3x N sqrt(gamma) [ Mpl^2/2 (R3 + Kij Kij - K^2) + F(t,N) + C_acc a_i a^i ]`,

with

`a_i = D_i ln N`.

`F(t,N)` is the unitary-gauge form of the fixed Khronon/P(X)-type clock background. Its time dependence represents the background clock trajectory after unitary gauge fixing; it is not an independent coefficient fit at each epoch.

The previous benchmark `rtk/route_b_spatial_covariant_benchmark.py` established the Stückelberg long-wave coefficients, the acceleration-generated `(grad dot pi)^2` operator, the velocity-Hessian lapse null direction and the generic three-DOF spatially covariant constraint count. The present appendix adds the full flat-FLRW scalar lapse+shift reduction and an exact production-background coefficient identity.

---

## 1. Production DBI-Khronon state variables

Production uses

`x = x0/a^3`,

`s = sqrt(1 + lambda_D x^2)`,

`r = x/s`,

`t = x/(s+1)`,

`Q = 1+r`.

The background code defines

`rho_8piG = 2 mu_K^2 x (1+t)`,

`p_8piG = 2 mu_K^2 r t`,

`c_a^2 = r/[s(s+x)]`,

`M_K = mu_K Q s sqrt(s)`.

Canonical implementation: `rtk-class-build:rtk/khronon_background.c`.

### Enthalpy identity

First,

`rho_8piG + p_8piG`
`= 2 mu_K^2 [x(1+t)+r t]`.

Divide the bracket by `x`:

`1+t+(r t/x)`.

Since

`t=x/(s+1)`, `r=x/s`,

we have

`t + r t/x`
`= x/(s+1) + x/[s(s+1)]`
`= x/s`
`= r`.

Therefore

`G_8piG := rho_8piG+p_8piG = 2 mu_K^2 x Q`.

Status: GREEN exact algebra.

---

## 2. Exact kinetic-coefficient identity

For a P(X)-type scalar, define the usual adiabatic quadratic coefficients

`G = rho+p`,

`K = (rho+p)/c_a^2`.

Using the production expression for `c_a^2`,

`c_a^2 = r/[s(s+x)]`.

But `x=r s`, so

`s+x = s(1+r)=s Q`.

Hence

`c_a^2 = r/(s^2 Q) = x/(s^3 Q)`.

Therefore

`K_8piG`
`= [2 mu_K^2 x Q] / [x/(s^3 Q)]`
`= 2 mu_K^2 Q^2 s^3`.

Production also has

`M_K^2 = mu_K^2 Q^2 s^3`.

Thus the exact identity is

**`K_8piG = 2 M_K^2`.**

No limiting approximation is used.

Because the reduced Planck mass satisfies

`Mpl^2 = (8 pi G)^(-1)`,

we have

`K_phys = Mpl^2 K_8piG`.

The spatial-covariant benchmark requires

`C_acc = K_phys/(2 M_K^2)`

to generate the RTK kinetic factor.

Using the identity above,

**`C_acc = Mpl^2` exactly.**

This coefficient is independent of `a`, `x`, `lambda_D`, `mu_K` and the observationally fitted cosmological point once the gravitational normalization is fixed.

This is the central new fixed-action bridge: the production RTK background itself selects a constant acceleration coefficient rather than an epoch-dependent fitted function.

Status: GREEN exact symbolic identity; physical carrier status remains YELLOW pending the rest of the C7/C9/PPN gates.

---

## 3. Fixed state-function representation

Since

`r=x/s = x/sqrt(1+lambda_D x^2)`,

one has

`s^2(1-lambda_D r^2)=1`.

Thus

`s=(1-lambda_D r^2)^(-1/2)`

on the positive branch.

The target functions become

`c_a^2(r) = r(1-lambda_D r^2)/(1+r)`,

`M_K(r) = mu_K(1+r)(1-lambda_D r^2)^(-3/4)`.

Therefore the evolving sound speed and dispersion scale are not independent epoch-by-epoch numbers: they lie on one fixed state-function curve generated by the same `lambda_D,mu_K` background.

Status: GREEN algebra.

---

## 4. Scalar FLRW perturbation variables

Use flat FLRW and comoving/unitary gauge:

`N = 1+n`,

`N_i = partial_i psi`,

`gamma_ij = a^2 exp(2 zeta) delta_ij`.

Let

`y = p^2 = k^2/a^2 > 0`.

The homogeneous `p=0` mode is a separate background/gauge sector and is not part of the finite-momentum scalar-dispersion theorem.

After expansion to quadratic order, using the background equations to remove tadpole/background-mass pieces, the scalar Lagrangian density divided by `a^3` is

`L2/a^3 =`

`-3 Mpl^2 dot(zeta)^2`
`+6 Mpl^2 H n dot(zeta)`
`-3 Mpl^2 H^2 n^2`
`+K_phys/2 n^2`
`+C_acc y n^2`
`+2 Mpl^2 y n zeta`
`+Mpl^2 y zeta^2`
`-2 Mpl^2 y [dot(zeta)-H n] psi`.

Origin of the terms:

- `KijKij-K^2`: metric kinetic terms and the lapse/shift mixing;
- `F(t,N)`: `K_phys n^2/2` after the background `F_N,F_NN` identities;
- `R3`: `2 Mpl^2 y n zeta + Mpl^2 y zeta^2`;
- `C_acc a_i a^i`: `C_acc y n^2`.

This equation is the action-level object from which the constraint elimination is performed; the coefficients are not fitted Schur variables.

---

## 5. Exact momentum/shift constraint

Vary with respect to `psi`:

`dL2/dpsi = 2 Mpl^2 y [H n-dot(zeta)] = 0`.

For `y>0`,

**`n = dot(zeta)/H`.**

The acceleration operator contains no shift, so it does not modify this momentum constraint.

Varying with respect to `n` and then using the momentum constraint gives

`psi = -zeta/H - [K_phys+2 C_acc y] dot(zeta)/(2 Mpl^2 H^2 y)`.

The inverse `y` in the nondynamical shift solution is not a nonlocal propagating degree of freedom; it is the usual scalar constraint solution. The reduced action below is local in the retained scalar field.

Status: GREEN exact quadratic constraint algebra.

---

## 6. Reduced action before the final integration by parts

Substitute

`n=dot(zeta)/H`.

The pure Einstein-Hilbert time-kinetic terms cancel, leaving

`L_after/a^3 =`

`[K_phys/2 + C_acc y] dot(zeta)^2/H^2`
`+2 Mpl^2 y zeta dot(zeta)/H`
`+Mpl^2 y zeta^2`.

The remaining mixed term is integrated by parts using

`y=k^2/a^2`.

Since

`a^3 y/H = a k^2/H`,

one obtains

`2 Mpl^2 a^3 y zeta dot(zeta)/H`
` -> -Mpl^2 a^3 y [1-dot(H)/H^2] zeta^2`.

Adding the explicit `+Mpl^2 y zeta^2` term leaves

`Mpl^2 y dot(H) zeta^2/H^2`.

For the single-clock background,

`G_phys = rho+p = -2 Mpl^2 dot(H)`.

Therefore the exact reduced quadratic action is

`S2 = 1/2 ∫ dt d^3x a^3/H^2 { [K_phys+2 C_acc y] dot(zeta)^2 - G_phys y zeta^2 }`.

Status: GREEN exact reduced action within the stated candidate/action/gauge assumptions.

---

## 7. Exact RTK match with the production coefficient identity

Insert

`C_acc=Mpl^2`

and

`K_phys = 2 Mpl^2 M_K^2`.

Then

`K_phys+2 C_acc y`
`= K_phys [1+y/M_K^2]`.

Hence

**`S2 = 1/2 ∫ a^3/H^2 [ K_phys(1+p^2/M_K^2) dot(zeta)^2 - G_phys p^2 zeta^2 ]`.**

The scalar dispersion is therefore

`omega^2 = (G_phys/K_phys) p^2/[1+p^2/M_K^2]`.

But

`G_phys/K_phys = c_a^2`.

So finally

**`omega^2 = c_a^2 p^2/(1+p^2/M_K^2)`.**

This is exactly the production RTK law

`c_s^2(k,a)=c_a^2(a)/[1+(k/(a M_K))^2]`.

No expansion in `p/M_K` and no epoch-by-epoch coefficient fit is used.

---

## 8. Explicit constraint matrix and relation to the Schur pole program

For nondynamical variables `x_c=(n,psi)`, the quadratic constraint block is

`M_nn = K_phys - 6 Mpl^2 H^2 + 2 Mpl^2 y`,

`M_npsi = 2 Mpl^2 H y`,

`M_psipsi = 0`.

Therefore

`det M = -4 Mpl^4 H^2 y^2`.

This is important for interpreting the earlier C8 single-linear-constraint-pole theorem.

The present carrier does **not** claim that the RTK rational dispersion arises from a strict linear pole in the lapse+shift determinant. Instead, powers of `y` in the constraint inverse cancel against the mixing/source structure, while the local acceleration operator directly generates the momentum-dependent kinetic coefficient.

Therefore the earlier necessary condition

`D2=0`

for a candidate that specifically claims a strict single-linear **constraint-determinant** denominator is not a rejection criterion for this mechanism. The two mechanisms answer different algebraic questions.

This resolves a potential false conflict between the Schur diagnostic program and the direct local acceleration carrier.

---

## 9. What this result establishes

If the CI artifact passes, this establishes for the stated action and perturbation sector:

1. a fixed local preferred-foliation action-level mechanism;
2. exact flat-FLRW lapse+shift elimination;
3. exact production DBI identity `K_8piG=2 M_K^2`;
4. constant required acceleration coefficient `C_acc=Mpl^2`;
5. exact quadratic scalar RTK kinetic kernel for all positive production background states;
6. exact RTK rational scalar dispersion at finite physical momentum.

This is stronger than the prior BPS pole-only embedding because it directly reproduces the RTK off-shell scalar kinetic normalization for `zeta` rather than merely sharing the same on-shell pole.

---

## 10. What remains open

This is **not yet the final C7 covariant completion**.

Still required for the same action/parameter identification:

- minimally coupled matter/source response and gauge-invariant transfer-function map;
- PPN/Newton normalization with the same coefficient tuple;
- GW/tensor constraints;
- compact-object/universal-horizon behavior;
- strong-coupling/EFT cutoff;
- radiative stability;
- nonlinear constraint behavior on backgrounds beyond the controlled FLRW branch;
- relation to the causal retarded RT auxiliary-history sector.

Existing negative results for narrower direct U-DHOST identifications must not be silently discarded; instead this spatially covariant carrier must be explicitly mapped into those observables to determine whether it lands in the excluded parameter branch or escapes through a different operator/source structure.

---

## 11. Provenance

Prior benchmark:

- `rtk-class-build:rtk/route_b_spatial_covariant_benchmark.py`.

Production background source:

- `rtk-class-build:rtk/khronon_background.c`.

New executable theorem:

- `rtk-class-build:rtk/route_b_spatial_covariant_flrw_exact_match.py`;
- theorem commit `36c30a9b94ad120bfe461d93057daf57db8d14dc`.

CI workflow:

- `main:.github/workflows/rtk-route-b-spatial-covariant-flrw-exact-match.yml`;
- workflow commit `7a7ced83037885988e5496bbf327425a2a69d4a2`;
- launch commit `c1810cdf26b3fc07cba9dc231d5f7a7f46fe3d95`.

Promote the appendix status from YELLOW only after inspecting the CI artifact rather than merely seeing a workflow success flag.
