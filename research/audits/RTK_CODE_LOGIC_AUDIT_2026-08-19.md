# RTK code and logic audit — 2026-08-19

This checkpoint records defects and interpretation risks discovered while B4/B9 heavy scientific runs were active. It separates scientific-result validity from control-plane/reproducibility issues and prevents already-resolved bugs from being rediscovered or silently reintroduced.

| Item | Severity | Finding | Action / current status | Scientific effect |
|---|---|---|---|---|
| B4 Hessian best-point selection | HIGH gating bug | Historical `neutrino_stationarity_hessian.py` computed `eff.best_exact` before evaluating the later `newton_trust_k01` exact point, so the production-eff minimum could in principle depend on build order. | Fixed in commit `4d2416c02979650e48e8f53cf67da4ae07079992`: final best selection occurs only after both Newton candidates. Historical LCDM base/half artifacts were independently scanned across all 75 exact points each. | No change to accepted LCDM result: center remains true all-point minimum at both scales. Active RTK run predates fix, so its `points.jsonl` must be rescanned before gating. |
| B9 fixed-center persisted provenance | HIGH provenance | Hand-persisted B9 result had incorrect artifact digest, run timestamps and `default_selfcheck_loglike`; cosmological scores were correct. | Corrected against primary artifact `9377441941` in commit `278c3c6d52da3df3b1b0ab8660a3116719721a95`. | No change to LCDM/RTK fixed-center lensing scores or raw diagnostic delta. |
| B9 boundary interpretation | HIGH gating semantics | Worker status could be `REOPTIMIZATION_BOUNDARY_HIT` while human-readable `next_gate` unconditionally mentioned Hessian certification. | Added `B9_BOUNDARY_INTERPRETATION_GUARD_v1.md`: shared-axis boundary requires same-width translated reoptimization; log-lambda boundary requires B9-specific tail/profile; no boundary point may be treated as interior. | Prevents premature local-minimum/closure claim; does not change exact scores. |
| B9 lensed-vs-unlensed spectra | HIGH interface risk, resolved | Needed proof that ordinary non-CMB-marginalized Planck lensing clik expects the standard lensed `Cl` theory product. | Official Cobaya `PlanckClik` + CLASS provider audited: `Cl` -> CLASS `lCl`, `lensing=yes`, `lensed_cl`; unlensed spectra use distinct `unlensed_Cl/raw_cl`. Persisted `B9_LENSED_CL_INTERFACE_GUARD_v1.md`. | Current use of `cl_lensed.dat` is correct; no restart required. |
| B9 CLASS `phiphi` units | HIGH interface risk, resolved | Needed proof that class-format `phiphi` does not carry an extra CAMB-like power of `l(l+1)`. | Pinned CLASS commit `36cf...` audited: `output_one_line_of_cl()` multiplies every class-format spectrum by `l(l+1)/(2pi)`. Adapter's division is exact inverse. Guard updated in commit `e5e16fc4a4ab6baeef85a331120b98cedc14ec4e`. | Current `phiphi` C_l payload is correctly normalized. |
| Planck `A_planck` semantics | MEDIUM/HIGH interpretation | All four project Planck products declare `A_planck`; distributed defaults differ (`1.000442` for lowT/Plik-lite, `1.0` for lowE/lensing). A5/B9-v1 copy each product's own `default_par`, so they are conditional-default rather than shared-nuisance-profiled objectives. | Four-product contract run `32302366002`, artifact `9383486257`; durable result `B9_PLANCK_CALIBRATION_CONTRACT_RESULT_v1.json`; interpretation guard and preregistered shared-`A_planck` profile protocol created. | A5/B9-v1 exact scores remain valid for their frozen conditional-default objective; do not call them fully shared-calibration-profiled Planck likelihoods. |
| B9 nonlinear lensing | MEDIUM physics/numerical robustness | Production `make_ini` sets `lensing=yes` but no CLASS nonlinear method. Thus B9-v1 is a linear-lensing calculation. | Pinned RT-CLASS supports `halofit`; `output_pk()` uses linear `pk.dat`, so a Halofit lensing variant can preserve BOSS linear-growth semantics. Preregistered `B9_HALOFIT_LENSING_ROBUSTNESS_PROTOCOL_v1.md`. | B9-v1 remains reproducible but is conditional on linear lensing. A separately scoped Halofit robustness test is earned after B9-v1 localization/certification. |
| CLASS smoke false failures | MEDIUM CI | Research-branch smoke ran stock CLASS before creating `output/`, producing false red CI after successful compile/link. | Added `mkdir -p output`; validated run `32300750240` SUCCESS. Auto-trigger now path-scoped to Khronon C/H or workflow changes. | No physics/source regression. Red noise and wasted Actions reduced. |
| Full CLASS build on every main push | MEDIUM resource | Heavy full diagnostic checked out research branch but ran on every main control/theory push. | Main workflow restricted to explicit trigger/manual run. | Frees Actions capacity; no reduction of source-change smoke coverage. |
| Legacy sparse AI agent | HIGH control-plane | `main:rtk/agent/frontier_state.json` contains historical sparse Stage4D3 score and legacy scheduled agent had write/dispatch permissions. | Automatic triggers retired; workflow read-only/manual fail-closed. Regression run `32300974527` SUCCESS. | Prevents stale sparse state from dispatching or overriding dense production research. |
| Runtime snapshot race | MEDIUM control-plane | Bot wrote runtime snapshot into `rtk-class-build`, while interactive scientific commits also wrote branch tip; snapshots could be lost/overwritten or race scientific state. | Dedicated branch `rtk-runtime-state` created; snapshot workflow moved there in main commit `2827f1d18298d7dcbd2a04c1fec68f42b68a2727`. First isolated snapshot successfully records current Actions state. | Separates ephemeral monitoring from scientific state and removes branch-write race. |
| Simple auxiliary-vector completion | THEORY narrowing | A derivative-free unconstrained `B_i` exactly integrates out to `+a_i a^i/(2M^2)`. | Machine theorem run `32300929835`; durable result saved. | Does not provide a genuinely new completion or evade effective-action degeneracy. |
| Retarded RT auxiliary homogeneous modes | THEORY clarification | Localized second-order RT auxiliaries formally have homogeneous constants, but retarded value+normal-derivative data fix them uniquely on a well-posed branch with nonzero Wronskian. | Machine lemma run `32301071093`; durable result saved. | `U,V,Z` must not be counted as freely tunable dark-fluid IC modes; full coupled DOF theorem remains open. |
| Reduced mixed-kinetic scalar kinematics | THEORY result | Exact target branch has `omega=c_s q/sqrt(1+q^2/M^2)` and `v_g=c_s/(1+q^2/M^2)^(3/2)`. | Successful rerun `32302480967`, artifact `9383521326`; durable result saved. | Real reduced branch for positive `M^2,c_s^2`; not full hyperbolicity/causal-cone/strong-coupling proof. |

## Mandatory follow-up guards

1. When B4 RTK run `32290108210` completes, compute the true production `score_eff` minimum across every row of `points.jsonl`, including `newton_trust_k01`, before deciding recenter/PD progression.
2. When B9 run `32289834876` completes, apply the boundary guard before any Hessian workflow; do not infer interior stationarity from optimizer success alone.
3. B9-v1 final language must state both conditional distributed-default calibration and linear-lensing semantics.
4. Shared-`A_planck` and Halofit variants are separately preregistered robustness tests and must not be started before B9-v1 resolves its current localization/stationarity dependency.
5. Never use `main:rtk/agent/frontier_state.json` as a production scientific state source.
6. Runtime monitoring should be read from `rtk-runtime-state:research/runtime/active_runs_snapshot.json`; durable scientific state remains on `rtk-class-build`.

**Overall audit conclusion:** no discovered code/provenance defect invalidates the already closed A5, B6, B10, or B4-LCDM results after primary-artifact rechecks. B4-RTK and B9 remain active and explicitly guarded against the newly discovered gating/interpretation risks. Fundamental C7/C8/C9 questions remain open.
