# RTK C10.65i–C10.65m frontier checkpoint

UTC checkpoint: 2026-08-26T01:28Z
Branch: `rtk-class-build`
Scope: completed-U1 scalar initial-condition / matching frontier. This checkpoint does not close C9 radiative protection, same-full-action background normalization, massive-neutrino coverage, or spectra/likelihood.

## C10.65i — coupled snapshot nullity with symbolic C2

Classification: `C10_65I_COUPLED_COEFFICIENT_NULLITY_REMAINING_TEMPORAL_AMPLITUDES_8_SCOPED`.

After corrected leading adiabatic conditions, leading comoving regularity, algebraic A->Hamiltonian->momentum projection, TCA photon-shear elimination, and quotienting one overall normalization, the fixed-C2 snapshot has eight unresolved temporal directions:
`D_b2, D_g2, D_ur2, delta_khr2, R_gb0, R_urb0, R_kb0, S_ur0`.
Releasing C2 adds exactly one matching direction. The exact core rank minor is `H/(1+w_khr)`.

## C10.65j — finite local jets do not select the branch

Classification: `C10_65J_FINITE_LOCAL_TEMPORAL_JET_INSUFFICIENT_NONLOCAL_BRANCH_REQUIRED_SCOPED`.

For a regular first-order linearized system, every finite jet at one onset is a graph over the onset state. The zeroth jet block is the identity. Introducing derivative variables adds identity blocks in their own columns. Therefore any finite stack of local derivatives leaves onset-state nullity unchanged. Exact-rational checks were executed through fifth order with noncommuting time-dependent coefficient matrices, including a direct eight-dimensional check.

Consequence: do not attempt to obtain growing-mode uniqueness by stacking more local derivatives at `a_on`.

## C10.65k — no certified pre-onset backward interval

Classification: `C10_65K_NO_CERTIFIED_PRE_ONSET_BACKWARD_INTERVAL_UV_MATCH_REQUIRED_SCOPED`.

The four exact low-k seed anchors `1e-5, 3e-5, 1e-4, 3e-4 Mpc^-1` all have persisted production support beginning exactly at
`a_on = 0.0002203229136467` (`tau ~= 87.89051579644 Mpc`). The common certified backward width in `a` is therefore zero.

The analytic Khronon background can be evaluated for positive `a`, but that alone does not certify the coupled completed-U1 primordial background. The preferred metric projector needs supported H/ordinary/total source background data. Historical CLASS metric potentials remain read-only controls and must not be substituted for the completed-U1 backward solution. Same-full-action Friedmann/gravity normalization remains an independent open gate.

Consequence: the unresolved seed must remain explicitly pre-EFT/UV matched unless a new independently certified earlier completed-U1 background branch is constructed.

## C10.65l — invariant matching interface

Classification: `C10_65L_UV_MATCHING_INTERFACE_BASIS_PASS_SCOPED`.

Minimal normalized boundary vector (nine coordinates):

1. `A2 = D_b2`.
2. `E_gb2 = D_g2-(4/3)D_b2`.
3. `E_urb2 = D_ur2-(4/3)D_b2`.
4. `E_khr2 = [I_khr-J_b]_{O(k^2)}`.
5. `R_gb0 = V_g0-V_b0`.
6. `R_urb0 = V_ur0-V_b0`.
7. `R_khrb0 = V_khr,pref0+B0-V_b,N0 = V_khr,N0-V_b,N0`.
8. `S_ur0 = lim_{k->0} sigma_ur/k^2`.
9. `C2 = [3a^2 delta_mu+3H Q]_{O(k^2)}`.

Bridges used:
`Psi_N=psi_pref-HB`,
`delta_N/(1+w)=delta_pref/(1+w)-3HB`,
`theta_N=theta_pref+k^2 B`,
and the exact C2 source-coordinate invariance proved in C10.65h.

The density-gradient reparameterization has determinant `1/(1+w_khr)` and is invertible on the certified branch. Important correction: never compare a preferred Khronon velocity directly to a Newtonian baryon velocity without the `+B0` bridge.

A diagnostic regular-control subfamily is defined (not derived): set all three entropy-gradient coordinates and all three relative-velocity coordinates to zero; supply `A2, S_ur0, C2` externally.

## C10.65m — numerical historical phenomenological control

Classification: `C10_65M_HISTORICAL_CONTROL_MATCHING_VALUES_PASS_SCOPED`.

Pinned read-only CLASS reconstruction on the four exact low-k onset anchors gives:

- `A2 = -1120.906563855608`
- `S_ur0 = 298.90841588141416`
- `C2 = -1.314425482950032`

Nested smallest-3 vs all-4 fit relative disagreements:

- A2: `1.1975203424885014e-08`
- S_ur0: `2.7004142571338068e-14`
- C2: `2.9402199593063025e-07`

Direct `C=C0+C2 k^2+C4 k^4` fit gives `C0=9.813011103277725e-17`, confirming the leading comoving cancellation numerically.

The full diagnostic matching vector is therefore
`(A2, E_gb2, E_urb2, E_khr2, R_gb0, R_urb0, R_khrb0, S_ur0, C2)` =
`(-1120.906563855608, 0, 0, 0, 0, 0, 0, 298.90841588141416, -1.314425482950032)`.

This vector is explicitly `HISTORICAL_PHENOMENOLOGICAL_CONTROL_ONLY`; it is not a microscopic UV derivation and not a low-energy completed-U1 prediction.

## Next frontier

C10.65n should construct the conditional completed-U1 onset seed from the C10.65m control vector. Required order:

1. Fix historical normalization `J_ad0 ~= -3` only as the declared control normalization.
2. Reconstruct ordinary leading and O(k^2) density invariants from A2 and zero relative entropy gradients.
3. Reconstruct neutral density coefficients from `Delta I_iso0=0` and `E_khr2=0` using the preferred A-projector.
4. Use leading comoving regularity to determine the aggregate preferred velocity, not an arbitrary velocity assignment.
5. Obtain `psi_pref0` and its time derivative from the differentiated dressed ordinary A source; then solve the Hamiltonian for `phi_pref0`.
6. Reconstruct `B0=(C2+2 Pcal psi0-E_th phi0)/(2H)`.
7. Use the source-locked compromise_CLASS TCA helper for photon shear; do not replace it by theta_g=theta_b as an algebraic constraint.
8. Include massless-UR shear from S_ur0.
9. Run all 3x3 `(lambda_HL,M_c)` diagnostic points and low-k/onset-shift consistency checks before any conditional full-Boltzmann spectra/likelihood run.

Do not import historical CLASS phi/psi as completed-U1 metric values during C10.65n.
