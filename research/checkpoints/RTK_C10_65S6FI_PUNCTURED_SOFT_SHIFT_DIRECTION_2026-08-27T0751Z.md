# C10.65s6fI punctured-soft shift direction checkpoint

Classification: `C10_65S6FI_PUNCTURED_SOFT_SHIFT_DIRECTION_DEPENDENT_PASS_SCOPED`.

The complete shift-dependent cubic ADM kinetic action (beta-linear plus beta^2 zeta kernel variation) was reduced with the same frozen projectable action. After solving beta at linear order and only then taking q->0, the soft coefficient contains

`9(1-lambda_HL) omega + 4 mu^2 (k v_g - 3 omega)`.

For the frozen n=2 dispersion,

`3-n_g = 2 M_U^4/(M_U^4+k^4) + k^2/(M_K^2+k^2) > 0`,

so `k v_g-3 omega = omega(n_g-3)` is strictly nonzero for finite positive scales. The punctured q->0 limit is therefore direction dependent.

This is not a final ZERO/NONZERO result. It means the exact projectable homogeneous q=0 channel cannot be silently identified with the punctured limit. Next gate: C10.65s6fJ homogeneous-vs-punctured channel selection theorem. k=0.03 production remains blocked.
