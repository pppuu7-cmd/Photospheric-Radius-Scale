# RTK frontier checkpoint — 2026-08-24 18:46 UTC / 21:46 Europe-Helsinki

This checkpoint is chat-independent and records only repository-authoritative results or explicitly marked pending gates.

## A5 matched cosmology

Current exact LCDM navigation/recenter1 point:

- `S_LCDM = 1049.3550570964142`
- source: exact `newton_trust_eff` improvement from the t=1.1 half stencil
- improvement over the t=1.1 center: `0.008263249722176624 > 0.005`
- parameters:
  - `As = 2.098490857524975e-09`
  - `Ob = 0.048382880062392684`
  - `Om = 0.25847022980776346`
  - `h = 0.6804976358942407`
  - `ns = 0.9664964612260075`
  - `zre = 7.67518361771309`
  - `lambda_D = 0`

Historical certified RTK local reference remains `S_RTK = 1050.249912429787`.
The current navigation-only raw gap is therefore `Delta S = +0.8948553333727887` (RTK-LCDM).

`A5_LCDM_T1P1_RECENTER1_BASE_RESULT_v1.json` is still pending. Do not freeze a new A5 pair or run model-selection statistics before the recenter1 base/conditional-half/fresh-tree/common-paired chain passes.

## B4 and B5

- B4 v4 half negative-mode exact rays remain pending; no persisted scientific result at this checkpoint.
- B5 BOSS linear scale-dependence gate remains pending; B5-SURVEY stays separate even after any B5-LIN pass.
- Do not classify missing result files as scientific failures.

## Weak-field same-action U(1)+RTK closure

Authoritative core quartet on the standard weak-field PPN branch:

- `gamma_PPN = 1`
- `beta_PPN = 1`
- `alpha1_PPN = 0`
- `alpha2_PPN = 0`
- `G_N = G` on the certified O(2) branch

Canonical aggregate: `research/theory_results/RTK_ROUTE_B_U1_CORE_PPN_QUARTET_CLOSURE_v1.json`.

Scope remains weak field, slow matter, rolling `Sigma=q t`, no independent incoming/homogeneous RTK scalar excitation in the PPN solution. Strong-field rotation, compact objects and UV/radiative questions remain open.

## C8 local-rest rank frontier

Two new authoritative exact subgates are now closed:

1. `RTK_C8_U1_LOCAL_REST_SCALAR_SPATIAL_PRINCIPAL_DEGENERACY_EXACT_PASS`
   - `P_X(X_star)=0`
   - `P_XX(X_star)=mu_K^2/(2 X_star^2)`
   - `c_s^2(X_star)=0`
   - fixed-metric scalar quadratic action: `L2/M_Pl^2 = mu_K^2 dot(phi)^2 + |grad dot(phi)|^2`
   - no quadratic `|grad phi|^2` restoring term.

2. `RTK_C8_U1_LOCAL_REST_FULL_SCALAR_QUADRATIC_RANK_ENHANCEMENT_EXACT_PASS`
   - finite `k>0`, flat branch, `nu=0`, `lambda_HL=1`, `beta0_bare=0`, `sigma1=sigma2=0`
   - U(1) A-constraint gives `zeta=0`
   - scalar shift gravity term cancels at `lambda_HL=1`
   - full RTK quadratic kernel becomes `(mu_K^2+k^2)[dot(phi)-n]^2`
   - lapse equation gives `n=dot(phi)`
   - reduced finite-k quadratic RTK scalar action is zero.

Interpretation: the exact local rest vacuum is a quadratic constraint-rank-enhanced surface relative to the regular rolling branch. This is a strong-coupling/constraint-bifurcation warning, not yet a numerical cutoff or a pathology theorem.

The next quartic target was frozen before its result:
`research/theory_targets/RTK_C8_U1_LOCAL_REST_CONSTRAINED_QUARTIC_TARGET_v1.json`.
Its CI gate is active. Expected question: whether the first nonzero fully constrained local-rest scalar action is the positive quartic purely spatial functional
`J [mu_K^2(-Delta)/(mu_K^2-Delta)] J`, `J=|grad phi|^2/2`.

## C9 naturalness

Scoped negative closure is authoritative:
`RTK_C9_U1_EXISTING_SYMMETRY_PROTECTION_NEGATIVE_CLOSED`.

The already-declared `U(1) x Diff(M,F)` plus internal Sigma shift symmetry does not protect `sigma1=sigma2=0`; the corresponding pure-gravity marginal operators are allowed. C9 therefore requires one explicit additional mechanism: Ward symmetry, counterterm-stable degeneracy, RG fixed surface, or a quantitative induced-coupling/tuning bound below a demonstrated EFT cutoff.

## Multi-model matched benchmark

Frozen before new alternative-model scores:
`research/benchmarks/RTK_MATCHED_MULTI_MODEL_BENCHMARK_PROTOCOL_v1.json`.

- LCDM / RTK: existing matched objective.
- wCDM: implementable on the current pinned CLASS branch with `w0_fld` free, `wa_fld=0`, `cs2_fld=1`, `Omega_fld` inferred by closure.
- full CPL `w0-wa`: currently blocked on this exact CLASS boundary because the pinned perturbation code explicitly rejects trajectories crossing `w=-1`. A non-crossing subset must not be labelled a full CPL benchmark.
- Full CPL requires a separately frozen crossing-safe PPF/fluid implementation or a new pinned solver boundary plus control replays.
- wCDM optimization must not start until the active A5 LCDM recenter chain provides the accepted common reference center.

## Strict next order

1. Inspect A5 recenter1 base result when persisted and follow its frozen recenter/half/ray rule.
2. Inspect B4 v4 half-ray and B5-LIN results; advance only on persisted scientific classification.
3. Inspect the C8 constrained-quartic result; if PASS, derive the first time-dependent nonlinear constrained order and test a minimal higher-spatial rescue.
4. After A5 refreeze, launch wCDM on the identical objective under the frozen benchmark protocol.
5. Design but do not silently substitute a crossing-safe CPL implementation.
6. Keep B9/B10 protocol-v1 closed and do not consume repeat compute.
