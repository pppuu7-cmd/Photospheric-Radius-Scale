# RTK C10.65s1 PASS / C10.65s2 frozen checkpoint

UTC: 2026-08-26T16:44:13Z
Branch: `rtk-class-build`

## Closed in this iteration

C10.65s1 executed successfully in GitHub Actions run `32989757332` under pinned CLASS upstream `36cf283628c4a3330ec9fd3d84239bf775f77317`.

Classification: `C10_65S1_FINITE_STATE_COMPLETION_AT_ONSET_PASS_SCOPED`.

Observed frozen-domain summary: 36 completed finite-state records (9 completion points x 4 exact low-k anchors), common `l_max_ur=17`, `RSA=0`, `UFA=0`, no resolution-limited active higher-UR modes. Dormant observer OFF-path numeric perturbation rows were SHA-256 identical to the pinned control on all four anchors. The result was persisted by Actions as commit `2128dc3fcfc70669ad8c3b4c65b6f289812c60b6` in `research/theory_results/RTK_C10_65S1_FINITE_STATE_COMPLETION_AT_ONSET_RESULT_v1.json`, with run/provenance fields and `threshold_changed=false`.

This closes only the finite-state completion-at-onset gate. It is not production-feedback evolution and is not a stability/spectra/likelihood result.

## Newly frozen next gate

Target: `research/theory_targets/RTK_C10_65S2_DIRECT_ONSET_ONE_STEP_PRODUCTION_CANARY_TARGET_v1.json`

Status: `FROZEN_BEFORE_IMPLEMENTATION`.

Frozen canary domain: first persisted C10.65n completion point (`lambda_HL=1.0016708437761068`, `M_c=9066.231026460177 Mpc^-1`) x exact anchors `k={1e-5,3e-5,1e-4,3e-4} Mpc^-1`, at `a_on=0.0002203229136467`.

S2 is deliberately narrow: deterministic opt-in direct onset handoff, exact OFF rollback, source-locked complete s1 initialization under CLASS state ownership, first production completed-U1 projector/RHS parity against certified r2 with the inherited frozen `5e-9` relative RHS bound, then exactly one accepted post-handoff integrator step per anchor. Preferred A/Hamiltonian/momentum constraint residuals are captured before/after the step, but s2 does not define a post-hoc finite-time drift threshold from the same data.

State ownership remains constrained by C10.65s0: the certified post-vector handoff carrier writes are only `delta_cdm` and `theta_cdm`; ordinary photon/baryon/UR/metric state must enter via `perturb_initial_conditions` / `perturb_vector_init`. Any implementation that needs additional post-vector handoff mutation is out of scope and requires a new architecture proof before scientific execution.

## Open frontier

- C10.65s2 implementation and one-step production-feedback canary: OPEN.
- Preferred A/H/M finite-short-interval stability criterion and multi-step test: OPEN; freeze only after s2 diagnostics, before the later run.
- Wider completion-point/k coverage and full production evolution: OPEN.
- Spectra/likelihood: BLOCKED on dynamic certification.
- Same-full-action primordial/background closure, massive-neutrino completion, C9 radiative naturalness, broader C7, B9: unchanged/open where previously open.

No DSIR material was read into or written into this RTK checkpoint.
