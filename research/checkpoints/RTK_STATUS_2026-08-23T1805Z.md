# RTK research status — 2026-08-23 18:05 UTC

Status: append-only checkpoint after B9 LCDM v7 eigenmode-ray audit and low-k U(1)/elliptic-filter refinement.

## Closed / reproducible results

- Production RTK/DBI scalar isolated fixed-state dispersion remains the exact rational form
  `omega^2 = c_a^2 p^2 / (1 + p^2/M_K^2)`.
- Elliptic auxiliary matter compensator has exact auxiliary Dirac reduction with zero auxiliary physical DOF and
  `a1_eff(k)=k_phys^2/(M_c^2+k_phys^2)`.
- Exact scale-separation limits: `a1_eff(0)=0`, `a1_eff(M_c)=1/2`, `a1_eff(infty)=1`; the 1% two-scale window exists iff `k_local/k_cos>=99`. `M_c` remains symbolic and unfitted.
- Corrected full-action bookkeeping uses `beta0_bare=0`; explicit rolling RTK `S_mix` supplies the full production lapse-gradient target. Historical simultaneous `beta0_bare=2 + S_mix` was a double count and is withdrawn for full-action use.
- On the regular projected FLRW phase-space background at fixed homogeneous canonical `p_Sigma`, the neutral RTK mixed sector gives no direct contribution to `{pi_N,H_perp}` for arbitrary lapse Fourier mode. This canonical statement does not erase the distinct static/PPN acceleration effect.
- Pure-gravity punctured-low-k baseline has nonzero off-diagonal coefficient `b2=2 eta0 P(d-1)/(d lambda-1)` for the regular domain `eta0!=0`, `P!=0`, `d>1`, `d lambda!=1`; exact `k=0` remains a background/global zero-mode degeneracy and is not used as the physical propagating-rank certificate.
- Pure-gravity `d` entry starts at `q^2` (`q=|k|^2`) with exact flat-FLRW coefficient from the DeWitt contraction.
- Filtered ordinary-matter low-k corrections are controlled by the exact resolvent identity and the subleading source remainder is bounded at `q^2/M_c^4` order.
- Ordinary multi-fluid scope is additive over ordinary matter species; the neutral RTK scalar is excluded from the compensator source by architecture.
- For the corrected action, the leading low-k 2x2 block is antisymmetric: `L=[[0,-t],[t,0]]`, `t=b2+x/M_c^2`, so leading rank fails only at the isolated cancellation `t=0`; the earlier sufficient condition `M_c^2>|x/b2|` is not necessary for local punctured-low-k rank.
- Exact finite-q off-diagonal filtering has been reduced to a one-dimensional root condition. For ordinary positive matter, `M_c^2>R`, `R=2(rho-tau_rho)/(eta0(d-1)M_Pl^2)`, excludes an off-diagonal zero for all `q>=0` in the exact filter symbol; `0<M_c^2<R` permits one finite-q off-diagonal zero. Such a zero is not automatically a rank loss because the full determinant is `F(q)^2+a(q)d(q)`.
- UV dependence of the flat-background direct lapse entry is localized: with `beta0_bare=0`, the first relevant pure-lapse combinations are `beta2+beta4` at `q^2` and `beta8` at `q^3`; the published `b,c,d` support is independent of these potential coefficients on the flat homogeneous background.
- Generic canonical matter adds one identified extra structure to `d`: `d_extra=a_eff^2 V {H0,tau_H}_m`; the barotropic/commuting-response sector is the special case where this term vanishes.

## B9 Planck-R3 lensing robustness

LCDM v7 base Hessian parent run: `32601673857`, center `S_B9=1058.2173424114785`. Base exact best improvement is zero, but the effective Hessian has one soft negative eigenvalue `-0.001225093918953372`.

Frozen v7 negative-eigenmode ray run `32638574993` completed successfully. Artifact `9493118573`, digest `sha256:684244ab58b7cd1d81cb2ec05f92b5ee68b12d290abb1b3059ed4c0011daa37c`.

Result classification: `B9_LCDM_RECENTER_V7_BASE_EIGENMODE_RAYS_NO_DESCENT_GT_0P005`.
The best tested ray point was `alpha=-0.5` with `S_B9=1058.250333871573`, i.e. an improvement of `-0.032991460094535796` (it is worse than the unchanged center). Therefore no v8 recenter is allowed by the frozen decision tree.

Next B9 gate: independent half-scale Hessian resolution audit at the unchanged v7 center. The base Hessian must still be described as non-positive-definite until that audit closes.

## Remaining classical U(1)/rank work

1. Finish and CI-certify the exact flat-FLRW barotropic block
   `B(q)=[[a(q),-F(q)],[F(q),d(q)]]`, with determinant `det B=F(q)^2+a(q)d(q)`.
2. Classify all positive-q roots on the admissible EFT domain while keeping `M_c`, `lambda_HL`, `beta2+beta4`, and `beta8` symbolic until justified.
3. Extend beyond the isotropic/barotropic regular slice: generic matter response, anisotropic stress, generic background rank, and intermediate/high-k sectors.
4. Only after the full reduced finite-k rank is controlled should the classical no-extra-gravity-scalar claim be promoted from conditional/local to a same-action statement.

## Remaining phenomenology / consistency gates

- Same-action Newtonian / PPN / Solar-System derivation retaining explicit `S_mix`.
- Tensor/GW sector: tensor kinetic sign, gradient sign, propagation speed and current GW constraints.
- EFT cutoff / strong-coupling scale and radiative stability. The exceptional `sigma1=sigma2=0` surface is still technically unnatural under the known parent U(1)-Hořava analysis absent extra protection; C9 is not passed.
- Compact objects / strong-field sector and universal-horizon issue.
- Nonlinear structure / screening / galaxy-scale phenomenology if the model is intended to compete beyond linear cosmology.
- Final matched inference after B9 robustness closes; current raw-objective score differences are not global evidence, AIC/BIC, posterior odds, or Bayes factors.

## Current scientific assessment

The project has moved beyond a pure cosmological-fit ansatz: the DBI/rational-dispersion sector, local U(1) constraint architecture, and elliptic scale-dependent matter projection are now mathematically linked with several exact canonical/Dirac gates. The main remaining theoretical bottleneck is no longer the auxiliary compensator itself but full same-action finite-k rank over the physical mode domain and subsequent PPN/GW/naturalness checks. No value of `M_c` should be selected merely to force rank or observational agreement before those gates are complete.
