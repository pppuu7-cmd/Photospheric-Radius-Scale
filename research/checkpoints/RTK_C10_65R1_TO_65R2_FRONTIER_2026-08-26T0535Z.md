# RTK C10.65r1 -> C10.65r2 frontier checkpoint

UTC checkpoint: 2026-08-26T05:35Z
Branch: `rtk-class-build`

## Confirmed state

- C10.65r1 is a scoped technical PASS only: `C10_65R1_IN_CLASS_COMPLETED_PROJECTOR_PARITY_PASS_SCOPED`.
- Latest successful conditioned-projector Action observed: run `32933994667`, head `a483bf25b0786e72c4b193f0e1b8f6a9126d5580`.
- Persisted r1 result reports max C-vs-independent-local-Python relative error about `1.04e-12`, max C-vs-detached-parent relative error about `1.71e-12`, and dormant OFF-path identity.
- C10.65q remains `C10_65Q_NUMERIC_BPRIME_SLIP_CLOSURE_PASS_SCOPED`; its persisted global checks include projector reproduction `3.69e-16`, 70-vs-100 dps Bprime convergence `5.94e-72`, slip-insertion Bprime invariance `9.56e-102`, and exact weighted photon/baryon slip cancellation in its high-precision representation.

## New frozen target

`research/theory_targets/RTK_C10_65R2_IN_CLASS_FIRST_RHS_DIAGNOSTIC_PARITY_TARGET_v1.json` was frozen before implementation in commit `cc03c5cf72a22e0ede5fa6fa55368609a4c494e4`.

C10.65r2 is diagnostic-only. It must construct a shadow first RHS from r1 projector values plus q derivative/slip closure on the exact 3x3x4 onset grid. It must not write `dy`, production metric variables, TCA switching state, collision coefficients, or species RHS.

Frozen new parity bounds: retain r1 projector regression <=5e-11; C-vs-independent-local first-RHS <=5e-9; C-vs-detached parent <=1e-6; Bprime invariance <=5e-9; weighted photon/baryon slip cancellation normalized residual <=5e-13. These are r2-specific double-precision implementation bounds; no q high-precision theorem criterion is weakened.

## Exact pinned CLASS convention recovered before r2 coding

From `dirian/class_public` at pinned SHA `36cf283628c4a3330ec9fd3d84239bf775f77317`, `source/perturbations.c` states for Newtonian gauge:

- `metric_continuity = -3.*pvecmetric[index_mt_phi_prime]`
- `metric_euler = k2*pvecmetric[index_mt_psi]`
- `metric_shear = 0.`

The same pinned source uses `metric_euler` additively in the baryon and photon Euler equations. Under the already-certified completed-U1 bridge notation used by n/o/q/r1, the shadow mapping is therefore:

- `metric_continuity_shadow = -3 * Psi_N_prime`
- `metric_euler_shadow = k^2 * Phi_N`

Do not swap `Psi_N` and `Phi_N`: the first is the spatial-curvature potential mapped to CLASS `phi`, while the second is the lapse/Newtonian-force potential mapped to CLASS `psi` in this bridge convention.

## Immediate next implementation step

Implement `c10_65r2_diag` as a dormant-by-default extension applied after the conditioned r1 patch in a disposable pinned CLASS tree. Reuse the r1 local projector quantities, evaluate a cancellation-safe directional derivative for `B_prime`, reconstruct `Psi_N_prime`, compute the full compromise_CLASS TCA slip and the shadow baryon/photon, UR and preferred-Khronon first derivatives, and append only diagnostic columns. Add static guards that the patch contains no `dy[...] =` assignments and no replacement of production `metric_continuity` / `metric_euler` assignments. Independently recompute the same RHS in Python and compare all 36 records to q/n/o parents under the frozen r2 target.

Only after r2 PASS may a separate controlled `a_on` state-handoff / one-or-short-step integration gate be frozen.

## Non-claims / still-open physical gates

- C9 radiative naturalness remains open.
- Same-full-action primordial/background closure remains open.
- Historical matching values remain phenomenological control inputs, not a microscopic derivation.
- No completed-U1 production feedback, state handoff, integration, spectra or likelihood claim follows from r1 or from freezing r2.
