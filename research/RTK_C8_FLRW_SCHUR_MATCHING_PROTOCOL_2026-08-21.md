# RTK C8 — FLRW Schur-Complement Matching Protocol

Date: 2026-08-21
Status: diagnostic protocol; not a covariant completion

## Question

Can a single fixed set of carrier coefficients reproduce the RTK scalar rational kernel on FLRW after the nondynamical lapse/shift constraints are eliminated, including both the constraint pole and its residue?

This protocol exists because the fixed-Minkowski obstruction cannot simply be promoted to FLRW: background quantities such as H can enter the constraint matrix and move the pole with epoch even when Wilson coefficients are fixed.

## Algebraic setup

Let q denote the Fourier variable and x=(delta N, chi) the nondynamical scalar constraints. Write

L = 1/2 x^T M(q,H) x + x^T J(q,H) zeta + 1/2 K0(q,H) zeta^2,

with

M = [[A,C],[C,B]],   J=(P,R).

Exact elimination gives

K_eff(q,H) = K0(q,H) - N(q,H)/D(q,H),

where

D = det M = A B - C^2,

N = B P^2 - 2 C P R + A R^2.

The pole is controlled by D=0. Pole matching alone is insufficient; the rational residue encoded by N/D must also match the RTK target.

## Linear-in-q diagnostic

For

A=a0+a1 q, B=b0+b1 q, C=c0+c1 q,

write

M(q)=M0+q M1,

with

M1=[[a1,c1],[c1,b1]].

Then

D(q)=D0+D1 q+D2 q^2,

D0=a0 b0-c0^2,

D1=a0 b1+a1 b0-2 c0 c1,

D2=a1 b1-c1^2=det(M1).

A strict single-linear-pole target requires not merely D2=0 but also D1!=0. Therefore:

1. det(M1)=0;
2. M1 cannot be the zero matrix, because M1=0 would force D1=0;
3. for a 2x2 matrix, M1 must consequently have rank exactly one.

Thus a strict nonconstant linear denominator imposes the exact necessary structural gate

rank(M1)=1.

This is stronger than recording D2=0 alone. It says that the q-gradient part of the two-constraint matrix can contain only one independent constraint-gradient combination at leading linear order in q.

This rank-one statement is only an algebraic property of the reduced constraint matrix. It must not be called DHOST degeneracy unless an explicit covariant action has been mapped to these coefficients and its full degeneracy/constraint algebra has been derived.

## Correct pole-residue normalization

For P=p0+p1 q and R=r0+r1 q, N is generically cubic. Let the strict linear denominator be

D(q)=D0+D1 q,

with simple pole

q_p=-D0/D1.

Polynomial division gives a polynomial part plus a simple rational pole. The unnormalised remainder is

N(q_p),

but the pole residue of N/D is

Res[N/D] = N(q_p)/D'(q_p) = N(q_p)/D1.

Since the Schur term enters the effective kernel with a minus sign,

K_eff = K0 - N/D,

the corresponding pole residue carried by the Schur subtraction is

Res[K_eff pole] = -N(q_p)/D1.

The normalized residue, not N(q_p) by itself, is the quantity to compare between two representations with differently normalized denominators. Matching only q_p or only the unnormalised remainder is insufficient.

## Required gates for an actual C8 candidate

1. **Action map** — derive A,B,C,P,R,K0 from one explicit fixed covariant/ADM action. No coefficient may be fitted independently at each epoch.
2. **Constraint-rank gate** — in the linear-in-q reduction, verify rank(M1)=1 whenever a strict nonconstant linear denominator is claimed; also verify no unintended singularity over the physical q-domain.
3. **Pole gate** — match the RTK pole location over more than one FLRW epoch with one fixed underlying coefficient set.
4. **Normalized-residue gate** — match -N(q_p)/D1, with sign convention stated, at the same epochs; matching D alone or N(q_p) alone is insufficient.
5. **Polynomial-part gate** — show that the polynomial quotient generated by N/D is correctly combined with K0 and does not introduce a mismatched local term.
6. **Kinetic/gradient gate** — after elimination, verify positive physical kinetic residue, gradient stability and hyperbolicity.
7. **DOF gate** — establish the degree-of-freedom count from the actual action/constraint algebra.
8. **IR/UV gate** — recover the tested RTK IR kernel in its validity domain and state the EFT cutoff above all used scales.
9. **PPN/GW gate** — check the same parameter set against the pinned Newton normalization, preferred-frame and GW constraints.

## Reproducible algebraic tools

Base Schur tool:

`rtk/route_b_flrw_schur_kernel.py`

Commit introducing it: `175adcc14bdfcdfc83218055dcbe4b0096545980`.

Rank/residue theorem diagnostic:

`rtk/route_b_flrw_schur_rank_residue.py`

Commit introducing it: `5029f5dd9bc62665063a926bc260a871bb9bb6f9`.

The exact-arithmetic tests now cover:

- exact Schur-complement elimination in a diagonal constraint matrix;
- A<->B, P<->R symmetry;
- D2=det(M1);
- the strict-linear implication rank(M1)=1;
- rejection of full-rank M1 and rank-zero M1 as strict nonconstant linear-denominator carriers;
- exact pole location q_p=-D0/D1;
- normalized residue N(q_p)/D1;
- the opposite sign of the Schur contribution in K_eff;
- invariance of the normalized residue under a common nonzero scaling of numerator and denominator.

## Interpretation rule

Passing these algebraic tools is not evidence for a complete theory. Their purpose is to reject impossible coefficient patterns early and to prevent a future carrier proposal from matching only the denominator while silently failing the residue or its normalization.

A C8 success claim requires one explicit fixed action to pass the rank, pole, normalized-residue, polynomial-part, stability, DOF, IR/UV and observational consistency gates together.
